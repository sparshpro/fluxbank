spring:
  application:
    name: advanced-features-service
  data:
    redis:
          host: localhost
          port: 6379
  datasource:
    url: jdbc:mysql://localhost:3306/advance_features_db?createDatabaseIfNotExist=true
    username: root
    password: Sairam@0919
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
        ddl-auto: update
        show-sql: true
        properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQLDialect
            #Dialect = Database-specific SQL translator used by Hibernate.
  security:
    oauth2:
      resourceserver:
            jwt:
              issuer-uri: "http://43.204.235.106:8085/realms/master"


      client:
        registration:
                  keycloak:
                    client-id: fluxbank-client
                    client-secret: Vw4KDFySPJB7hOrCPQW8j80FHIpYSZGU
                    authorization-grant-type: client_credentials
                    scope: openid
        provider:
          keycloak:
            token-uri: "http://43.204.235.106:8085/realms/master/protocol/openid-connect/token"

server:
  port: 8082

services:
  core-banking:
    url: http://localhost:8081



info:
  app:
    name: Core Banking Service
    description: Banking backend microservice
    version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    oauth:
      client-id: fluxbank-client
      client-secret: Vw4KDFySPJB7hOrCPQW8j80FHIpYSZGU
      use-basic-authentication-with-access-code-grant: false